git add .  (denna går på allt, man kan styra mer spec)
git commit -a -m "MEDDELANDE"
git tag -a v1.0.0 -m "Ett meddelande för denna taggen" (ändra version och meddelande)
git push
git push --tags


Vid problem vid körning "git push --tags" kör denna istället -> "git push -f --tags" (force)
! [rejected]        v2.0.0 -> v2.0.0 (already exists)
error: failed to push some refs to 'git@github.com:pellesune/bth_design.git'

20200503
git add .
git commit -a -m "First version of Krav 1 and 2 (kmom04 )for oophp-course"
git tag -a v4.0.1 -m "First version of Krav 1 and 2 (kmom04 )for oophp-course"
git push
git push --tags


20200502
git add .
git commit -a -m "Initial commit for kmom04 for oophp-course"
git tag -a v4.0.0 -m "Initial commit for kmom04 for oophp-course"
git push
git push --tags

20200430
git add .
git commit -a -m "Released with game dice 100 and redovisning for oophp-course"
git tag -a v3.0.1 -m "Released with game dice 100 and redovisning for oophp-course"
git push
git push --tags

20200419
git add .
git commit -a -m "Initial commit for kmom03 including fix for GuessException in kmom02"
git tag -a v3.0.0 -m "Initial commit for kmom03 including fix for GuessException in kmom02"
git push
git push --tags

20200415
git add .
git commit -a -m "Released with kmom02 for oophp-course"
git tag -a v2.0.3 -m "Released with kmom02 for oophp-course"
git push
git push --tags

20200415
git add .
git commit -a -m "Efter - Dokumentera PHP med phpdoc och phpDocumentor"
git tag -a v2.0.2 -m "Efter - Dokumentera PHP med phpdoc och phpDocumentor"
git push
git push --tags

20200413
git add .
git commit -a -m "Efter - Flytta spelet Gissa mitt nummer till me-sidan"
git tag -a v2.0.1 -m "Efter - Flytta spelet Gissa mitt nummer till me-sidan"
git push
git push --tags

20200412
git add .
git commit -a -m "Initial commit for kmom02"
git tag -a v2.0.0 -m "Initial commit for kmom02"
git push
git push --tags

20200408
git add .
git commit -a -m "Redovisningstext"
git tag -a v1.0.3 -m "Redovisningstext"
git push
git push --tags

20200408
git add .
git commit -a -m "Bygg en me-sida för oophp med ramverket Anax"
git tag -a v1.0.2 -m "Bygg en me-sida för oophp med ramverket Anax"
git push
git push --tags

20200331
git add .
git commit -a -m "Efter att fått igång cimage"
git tag -a v1.0.1 -m "Efter att fått igång cimage"
git push
git push --tags
